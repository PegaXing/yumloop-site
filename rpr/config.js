/* Rock Paper Rumble publisher pitch — configuration.
   rockpaperrumble-pitch.html reads this at load. Nothing here is secret: the page is unlisted, not private.

   Links. Empty string = not available yet: the page then shows the action as "on request" (a mailto),
   exactly like the Sightline pitch does. */
window.RPR_PITCH = {
  links: {
    buildWindows: '',                            /* Windows x64 build zip, e.g. a GitHub release asset on this repo. '' = "on request" */
    contact: 'mailto:yoni@yumloop.net?subject=Rock%20Paper%20Rumble'
  }
};
